# Program to find squares of numbers using map()

numbers = [1, 2, 3, 4, 5]
squares = list(map(lambda x: x**2, numbers))

print("Original List:", numbers)
print("Squares:", squares)

Output:

Original List: [1, 2, 3, 4, 5]
Squares: [1, 4, 9, 16, 25]

